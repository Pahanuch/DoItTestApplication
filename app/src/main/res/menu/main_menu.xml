<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_menu_logout"
        android:title="@string/menu_action_logout"
        app:showAsAction="ifRoom|withText"
        android:icon="@drawable/ic_exit_to_app" />

    <item
        android:id="@+id/action_menu_generate_gif"
        android:title="@string/menu_action_generate_gif"
        app:showAsAction="ifRoom|withText"
        android:icon="@android:drawable/ic_media_play" />

    <item
        android:id="@+id/action_menu_add_picture"
        android:title="@string/menu_action_add_picture"
        app:showAsAction="ifRoom|withText"
        android:icon="@android:drawable/ic_menu_add" />

</menu>